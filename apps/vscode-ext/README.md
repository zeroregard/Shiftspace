# @shiftspace/vscode-ext

VSCode extension — Phase 2.

This package will wrap `@shiftspace/renderer` in a VSCode webview panel, replacing the mock data source with real git commands and filesystem watchers.

See `CLAUDE.md` at the repo root for the full spec and development phases.
