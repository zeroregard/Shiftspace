{
  "name": "@shiftspace/core",
  "version": "0.1.0",
  "description": "Shared backend logic for Shiftspace extensions (git, actions, insights, MCP)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "minimatch": "^10.0.1"
  },
  "devDependencies": {
    "@shiftspace/renderer": "workspace:*",
    "@types/node": "^20.0.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
